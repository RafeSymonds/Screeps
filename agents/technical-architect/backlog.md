# Backlog

- Review where `Memory` contracts are extended outside `src/main.ts` and document gaps or migration risks. (Ongoing; initial scan complete)
- Review any upcoming `TaskRequirements` changes for global impact.
- Propose a lightweight change taxonomy for this repo (safe-local, cross-tick-risky, migration-required).
- Audit `ResourceManager` for potential race conditions during parallel task assignment.
