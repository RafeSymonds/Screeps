# Inbox

- From combat-specialist: Updated `DefensePlan` to protect active remotes and `AttackPlan` to proactively clear nearby invader cores. This will increase `defender` and `attacker` spawn requests when hostiles/cores are present. Also updated `intelStatus` to mark rooms with military hostiles as `DANGEROUS`, which will pause `RemoteMiningPlan` until they are cleared.
