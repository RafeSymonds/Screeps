# Backlog

- Review where `Memory` contracts are extended outside `src/main.ts` and document gaps or migration risks.
- Audit ownership boundaries between plans, task assignment, and spawn logic for areas likely to cause parallel-edit conflicts.
